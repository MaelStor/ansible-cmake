---
- name: Converge
  hosts: all
  become: yes
  become_user: root

  roles:
    - role: cmake
      vars:
        cmake_install_method: make_privileged
        cmake_make_test_skip: yes

# vim: set ft=yaml.ansible :
